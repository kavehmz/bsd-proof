# Global CM selector: completed bounded construction, independent PASS

Date: 2026-09-13. Owner /root/odd_rank_bridge, GPT-6 Astra/xhigh.
Own cm-global-selector-attack.md and this checkpoint only.
Initial checkpoint was saved before source work; the prospective
entries have now been replaced by this complete status.
Full BSD over Q remains the universal objective.

The full TEN-section proof passed /root/uniform_witness's independent
[review](review-cm-global-selector.md), SHA256
586f9bc57f6984e71dbf53ba2d235decf518ab9e6d4432c1fd831a68fa236fd1.
Reviewed mathematical proof SHA256 (later status/link edits editorial):
f1780e7f9f279faf0ac8e0661fee6370a94cceab07705dd028142c6017f57eef.
The full review was inspected. No mathematical correction or expansion
remains pending in these completed files.

Review precisions inspected and saved: the scalar-map statement now says
EVERY equivariant P→A_k map killsI; augmentation itself is nonzero.
At v∤p, the ordinary valuation residue is first over the source field
where rho modp^k is trivial. Actual ramified-coefficient transfer at the
base is retained and is not called an unramified scalar residue.
No class, norm, detector or scalar formula changed.

## Actual source and normalizations

Keep E:y²=x³+39x, K=Q(i), omega=dx/(2y), full basis P3,12 Q27,144.
p≥5, p≡1mod4, p∤2·3·13; pi=Psi(p), alpha=barpi unit atpi.
R∈{n_pP,n_pQ,n_p(P+Q)}, n_p=8#E(Fp), a p-unit; atp5 it is64.
beta_m=pi^m tau_m=Omega_infinity/(f0 barpi^m), f0=39(1+i)^3.
B_m=K(f barp^m), F_m=K(f p^m), F0=K(f), rho=(Psi^c)^-1.
The original rho vector, gamma_CM=2pr_rho gammaE+, twelfth theta,
q_a=12(a²-u_a), u_a=±a, a5 excepta7 atp5, remain unchanged.
All full sums/trace factors are unnormalized.
OverK_n the constructed class is a restriction fromK, so the original
finite q_(a,n,k) acts by its augmentation; X,Y→T remains a quotient.
The old iota,u(0), Smith/basis/Euler and period factors are retained.

The actual opposite point torsor Y_(R,m)=[barpi^m]^-1R and its
P=A_k[Y], I=keraug precede every cochain derivation. For S_m∈Y,
M_m=B_m(S_m) has [M_m:B_m]=p^m by the full opposite Kummer image.
It is unramified atpi: the isogeny[barpi] is finite etale there.
M_r∩B_m=B_r follows from Cartan conjugation -1 versus an abelian
intersection quotient. Thus [M_m:M_r]=p^(2(m-r)).
M_m∩F_m=B_m follows from unramified versus totally ramified atpi,
and M_m∩K_n=K.

## Exact class, norms, pullback and replication

The unit is the ACTUAL ratio
f_m(S)=Theta_a(beta_m+S)/Theta_a(beta_m).
Its full point norm is
Theta_a(tau0+R)/Theta_a(beta_m)^(p^m).
A_(R,m,k)=q_a^-1 Cor_(B_m/K)(delta_k(f_m) tensor t_rho)
lies inH1(K,P tensorT_pi/p^k), via finite etale Shapiro.

For m≥k its augmentation iszero. The denominator is killed byp^m;
the numerator is overF0 and its rho transfer is negated by the actual
opposite-ray homothety. No p-division is made. H0(K,T_k)=0 gives the
UNIQUE eta inH1(K,I tensorT_k). This also defines an actual finite
etale coefficient extension, not a claimed rational motivic class.

The FULL originalF_m transfer retains
(p−1)p^(m−1−n) res_(K_n/K) A_m, and the same factor oneta.
It iszero at m≥n+k+1. The smaller source was defined directly.

For m>r≥k, Norm_(M_m/M_r) f_m=f_r^(p^(m-r)).
The theta argument has a full barpi^(m-r) fiber with multiplicityp^(m-r).
Actual point-coefficient pushforward consequently gives
pi_(m,r) A_m=p^(m-r) A_r and pi_(m,r) eta_m=p^(m-r) eta_r.

There is a stronger geometric identity:
A_m=Sh_(Y/K)(s_m^* D_(m,k)), whereD is the already reviewed
origin-reduced punctured theta class. Its pointed isogeny law gives
A_m=j_(r,m) A_r, eta_m=j_(r,m) eta_r.
Herej sends a root to the SUM of all its lifts; pi*j=p^(m-r).
These are specific replications of an actual finite-level class.

## Exact local selector and nonzero global higher layers

The unique formal inverse S_m^0(R)∈E1(Qp) is G_Qp-fixed.
The coefficient map ev extracts the coefficient of THAT root.
Full Mackey/Shapiro localization of A_m, including every semilocal
coset and its SINGLE rho weight, is exactly the old U_pi(R) modp^k.
No point-degree factor, one-component trace or extra period is used.
Its Log_omega coordinate is the oldF_a(log_E R) modp^(k+1),
with the MATCHED Omega_j, not an unverified Katz-period identification.

The completed local certificate (read with BOTH reviews; not rerun)
therefore proves: one fixed R*∈{64P,64Q} has eta_(R*,m,2) NONZERO
for everym≥2. Its local value has exact order5 modulo25.
The global class may have larger order; augmentation remainszero.

For POINT jets of orderd, r≥k+floor(log_p d), m≥r+k implies
eta_m maps tozero inH1((I/I^(d+1)) tensorT_k).
OverK_n retain r≥n+1 as well. These point groups have orderp^r,
not the normalized ray generator's orderp^(r−1).
Pi-inertia acts trivially onY but by a nontrivial Teichmuller
character onT_k, so EVERY coefficient quotient here has H0=0.
Thus the higher-layer lift is UNIQUE.

Atp5,k2,m≥4, takingr=m−2,d=5^(m−3)−1 gives a unique NONZERO
lift inH1(K,I_m^(5^(m−3)) tensorT_pi/25), detected by the same
local class. Its next graded image is NOT asserted nonzero.
The actualselector satisfies ev((sigma−1)^j)=(-1)^j forj<p^m,
so it cannot factor through a fixed jet. It selects one lift,
whereas ev_r pi_(m,r) sums all lifts; this explains the different
local coefficient1 and global coefficientp tower laws.

The corrected first point moment is
A1(g)−kappa_(barpi^k)(R)(g) tensor g(v), withaugA=dv,
v=−augA(h)/2. The actual opposite CM factor is retained:
kappa_(barpi^k)=pi^k pr_barpi kappa_(p^k).
The Weil contraction hasH1(mu_(p^k)) target and the chosen order
e(t_barpi,t_pi); its unit adjoint isalpha^k. It iszero form≥2k
by the first-jet theorem, not a nonzero class inH1(T_pi).

## Actual proper-point corrections and surviving quotient

The already constructed compatible boundary family is
Xi_m=D_m−alpha^m c K_pi, c∈Zp. Pullback toY gives EXACTLY
eta_m^c=eta_m−c N_Y kappa_pi(R),
N_Y=sum_[S∈Y][S]. The alpha^m cancels usingbarpi^m S=R.
SinceaugN_Y=p^m=0modp^k, this is an actual GLOBALI-coefficient
correction. Its local evaluation is U_pi(R)−c kappa_pi(R).
No c is selected from a desired answer.

The norm vector has EXACTI-adic depthp^(m−k+1)−1 inA_k[Y]:
its coefficients arebinom(p^m,j+1), with the first nonzero one
atj=p^(m−k+1)−1 of valuationk−1. The group relation vanishes
through that degree, so the claim is over the true coefficient ring.

There is a surviving quotient of the PAIR of global classes at64P,64Q.
Putx=log64P,y=log64Q, a=x/5,b=y/5 mod25, and letL2=Log_omega/5.
The actual local functional
b L2(ev loc z_P)−a L2(ev loc z_Q)
kills the ENTIRE common-c correction line, and on(eta_P,eta_Q) equals
(yF_a(x)−xF_a(y))/25 mod25, of valuationEXACTLY1.
Proof: x,y,x−y havevaluation1; the numerator's leading term is
(C2/2)xy(x−y), valuation3, while everyd≥3 term hasvaluation≥4.
The all-degree integral Taylor bound and completed C2 unit prove this.
Thus every common-c correction leaves one of the two global classes
nonzero modulo25. Atm≥4 the statement also holds in the deep layers,
because the correction's norm-vector depth is larger.
The local detector is not claimed to be a rational motivic covector.

## Boundaries, exact gap and review handoff

Every finite valuation is retained:
v f_m=−12(v psi_a(beta_m+S_m)−v psi_a(beta_m)).
Ordinary etale Kummer residue interpretation excludesp.
Atpi the selected value is principal; atbarpi no fixed formal inverse
or finite-flat extension of the ramified rho twist is asserted.
The arithmetic support is finite level by level; no fixed globalS.
Primitive ray norms retain their imprimitive denominator.
The global full permutation scalar maps are all multiples ofaug
by transitivity, and therefore killI. This does not classify all
secondary maps on subquotients.

GAP CM-Global-Selector: an actual secondary arithmetic operation must
connect this coefficient/boundary tower to the ORIGINAL global
Selmer classw0/d_pi+d_barpi or its determinant, with the original
Kato/Bockstein normalization. No such equality follows from these
nonzero coefficient classes, their local detector or the proper-line
quotient. Preservec_(2,p)=iota(F20+F11+F02), F=(-1)^b u detA,
k_alpha, e_p, c_cmp=(156iOmega_p)^-1, g_p, factorp/(2Np).
C2 LOCAL is notc_(2,p). Rationality must precede ONE rational
framed element with real realization(L''/2)/(2Omega_E).
No Sha-finiteness, corank2 or complex rationality premise was used.

PrimaryKato1.3/1.10/15.8 was reread in the existing cache.
The finite Shapiro/cochain exact sequences and every new deduction
are detailed in the full proof. No old script, prime scan or new agent.
The reciprocal [mixed-quadratic Heegner audit](review-heegner-mixed-quadratic.md)
is also complete PASS, SHA256
88f67e22ed7c5c1dac4b9258a272b325498cb1e450fe72bb1e84c687e2a51455,
against its mathematical proof51d6f6e4... . No repair was needed there.

Next: return both completed results and final hashes to the coordinator.
No new mathematical task is dispatched in these files. Preserve the
explicit CM-Global-Selector and rationality-first BSD comparison above.
