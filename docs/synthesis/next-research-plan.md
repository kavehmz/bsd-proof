# Next research plan after tangential traces and global coefficient classes

Date:2026-09-13. Owner: root/coordinator. Full BSD over Q remains active.
This plan preserves rank equality, full Sha finiteness and the exact leading
term for EVERY elliptic curve over Q. None of the test-curve results is
substituted for that objective. All research agents remain Astra/xhigh.
Read current research-state.md and revalidate tools before any dispatch.

Ledger25 is complete: four new constructions with separate PASS reviews.
All ledger24 results and the independent local Taylor certificate remain
unchanged. The current thread scheduler reports usageLimited while the
account check permits ordinary use; revalidate before dispatching work.
The objective is unfinished and no mathematical impossibility is inferred.
No next mathematical construction is dispatched by this plan alone.

## 1. The remaining actual point–K2 motivic component

Read marked-rational-boundary-lift-attack.md and its PASS review. The
coefficient motive and real lift, exact rational connecting class, explicit
toric K2 corrections and full added-boundary localization are DONE.
The actual geometric PLUS/Tate projection of point×beta2 is ZERO.

The remaining products (P-O)×beta2 and (Q-O)×beta2 lie in the geometric
MINUS summand of h1_coho(E) tensor², realizing ordinary rank-three Sym²H1.
Compute their actual rational higher-Chow class or a nullcochain, and then
its relation to the actual added-boundary unit span. The full obstruction
is -j_B(epsilon0 tensor beta2,0), not merely its abelian projection.
A global coefficient restriction preimage remains another valid lift route.

The necessary constant-coefficient trace condition is
388chi_D=sum_s Tr(zeta_s RIGHT-CUPu(s)), where zeta represents the PROPER
projected obstruction. The degree3 boundary trace is negative on j_B and
positive on -j_B. Entire-fiber flat-pullback corrections have zero trace.
Do not assume every boundary correction has that form. Keep the nontrivial
Poincare fibers, all h_j markings and the rational projector denominators.

A zero real regulator does not prove a motivic relation. The ordinary
Somekawa formula tested previously has the wrong higher-Chow degree for
this product; do not use it by changing the degree. Any homotopy-module
or higher-symbol replacement must compute the actual shifted group.
Even this known-boundary lift would leave the spectral arithmetic selection.

The point-k2-derived-symbol proof now supplies exact models: the group is
degree-one Suslin homology of the projected rational transfer-sheaf tensor,
and the FULL H1 of the surface K3/K2/K1 Gersten complex. Its actual cochain
Theta_R has dTheta_R=z_R+D_beta-T_R D_beta. The explicit Xi_(P,Q) fills
the SECOND translation difference. The next target is a FIRST-difference
filling, or the full coefficient restriction preimage. Do not repeat the
stable/effective, sheaf-flatness or coniveau derivations already reviewed.

## 2. Arithmetic operation on the actual Poisson motive

Read poisson-hodge-framing-attack.md, poisson-path-motive-attack.md and
both reviews. The exact canonical real invariant and the ACTUAL source
motive, including labelled coincident endpoints and finite-base descent,
are DONE. Do not rebuild the MHS or its motivic source as open tasks.

The tangential source is now ACTUAL and its canonical invariant equals
the finite cusp limit, in ordinary degreezero with the correct dual-log
Tate sign. The weighted u=2 fiber trace further gives the actual
unnormalized source hSigma=-388cpi qF, with no cusp-constant subtraction.
The exact spectral operation is now pi/(4sqrt389 cpi) times the Petersson
pairing of I(hSigma) with Romega. All full-genus/Tate corrections remain.
Do not redo this tangential or weighted-trace construction as an open step.

Construct the arithmetic operation relating these specified source data,
the original Petersson/theta functional and the actual point-coefficient
family to the fixed point/K2/Tate determinant. Test the previously proved
Deligne-nilpotence obstruction before proposing an ordinary top-preserving
map: an actual motive does not remove that Hodge constraint. Additional
cohomology, coefficient extensions or a justified secondary operation need
actual maps and retained frames. Do not divide by an unknown regulator or
rescale a real class to obtain the BSD coefficient. Rationality of delta
is not a consequence of the source motive's rational definition.

## 3. CM: preserve the actual nonlinear local contribution

Read cm-asymmetric-ray-attack.md, cm-local-point-comparison-attack.md,
their reviews and the separate local Taylor certificate review. The direct
opposite-ray class, full-rho punctured class, proper A_k point line and
Zp boundary-lift ambiguity are DONE. Complete original transfer still
has its unused-ray factor; the smaller source has a point-shifting norm.
The local formal inverse is not a global ray-field section.

The ACTUAL local point coordinate is F_a(log_E R)=sum C_d(log_E R)^d/d!,
with explicit integral limits C_d and finite errors. Omega_j is matched
to the original Tate coefficient; any ratio to the Katz period is retained.
Do not infer additivity from norm compatibility or a rank-one target.
The new p5 test PROVED C2 a unit and a particular nonzero three-point
defect, with independent reproduction. At least one of the selected local
classes at64P or64Q is nonzero modulo25. Do not repeat the ruled-out
single-scalar comparison at64P,64Q,64(P+Q).

A concrete next calculation is to retain the actual polarized defect and
test a canonical quadratic extraction with PROVED integral divisibility
and error, rather than suppressing higher terms. Its purely local bilinear
part must be distinguished from the full global height: two global point
directions have a one-dimensional local logarithm image. All finite
principal-divisor corrections remain essential in any determinant claim.

The global-selector proof has now realized the local inverse in an ACTUAL
opposite point-division coefficient class. One fixed R* in{64P,64Q} gives
nonzero global classes mod25 for everym>=2, with unique lifts in
I^(5^(m-3)) form>=4. The pair survives EVERY common-c proper-point
correction, detected by the integral local annihilator of that line.
Norm projection is p perstep; pullback is actual replication. Ordinary
augmentation and the eventual first Weil contraction arezero. The exact
next target is a SECONDARY arithmetic operation on these specified classes
which produces the original Kato/Selmer output, retaining all boundaries
and tower factors. Do not ask again merely for a global enlarged coefficient
class or infer an ordinary scalar selector from it.
Arithmetic selection of the boundary counterterm, the original primitive-ray
Kato/Bockstein comparison, all-prime c2/index control and ONE rational frame
with the prescribed real period remain required.

## 4. Heegner: kill the particular factored native scalar

Read heegner-cofactor-lift-attack.md and its final PASS review. The fixed
A,B point-local lift defect and its bound from the order of kappa are DONE.
The source repair uses Kim's SAME modified-Selmer indices and positivity
M2-M3=a_3^-≥0; no ambient/modified-filtration identification is needed.
In the primitive case all fixed A,B,z lift integrally, but possible divisible
Sha directions are retained. In the nonprimitive case only the proved
scaled lift is available; do not cancel its p-power.

The actual cubic Heegner quotient gives R_i(kappa)=-a_j^+(eta_i)G_ij.
In the primitive clean case the unchanged z=alpha_z kappa gives
R_i(z)=-alpha_z a_j^+(eta_i)G_ij, with CT=-R_i/p^k.
The extra complex zero must now control this PARTICULAR product or the
canonical Prym cup, not an adjustable choice of A,B or a different point.
The exact quadratic companion eta_i and its global-over-H_i descent fix
the local H0 ambiguity. Do not replace it by a guessed complex derivative.
Nonprimitive and nonclean conditions, and the later mixed/top selection,
remain part of the target. No selected rational Kato point is supplied.

The actual biquadratic corner and its compact self-cup homotopy are now
constructed. In the primitive clean case there is NO FURTHER compact
mixed obstruction once the original first native products and the explicit
cross-local lambda conditions hold. The top ambiguity remains the actual
Rkappa torsor. Do not redo conditional mixed gluing; prove the first
products vanish from the extra complex zero and select the top through
an arithmetic comparison. Outside that scope retain the full compact
class and nonclean image groups exactly as in the new proof.

## 5. Review and continuity

New deductions require independent adversarial review. Preserve complete
proofs, source versions and new arithmetic verifiers. Re-run old numerical
certificates only for a changed dependency or a concrete unresolved concern.
Update research-state.md after a handoff, the ledger after a verified result,
and the manifest after final synthesis. The manifest is a snapshot, not
proof that a live agent or process has completed. Keep the goal active until
full universal BSD is actually proved or a genuine counterexample verified.
