# The actual biquadratic Heegner corner and native gluing

Date: 2026-09-13. Author /root/uniform_witness, GPT-6 Astra/xhigh.
**All seven sections passed [independent review](review-heegner-mixed-quadratic.md).**
[Restart checkpoint](heegner-mixed-quadratic-checkpoint.md).
Full BSD over Q remains active and unresolved.

We construct the next corner from the genuine Heegner point. Its
uncorrected mixed equation has an actual global solution. We then
compute the compact obstruction to correcting both first lifts.
In the primitive clean case a specific cancellation proves mixed
gluing once the first native conditions and the necessary cross-local
conditions hold. It does not prove those first conditions. The
remaining top ambiguity is still generated by an actual Heegner class.

## 1. Exact inherited input and scope

Use the [completed cofactor construction](heegner-cofactor-lift-attack.md)
and [its PASS review](review-heegner-cofactor-lift.md). Keep
n=p^k, R=Z/n, M=E[n], M_D=M tensor epsilon_K, the genuine inverse
Artin cocycles a_i,a_j in Z^1(U,R epsilon_K), and the fixed old primes
(i,j)=(ell,q). In particular p>=5, all coefficient denominators below
are units, and a_i takes value -1 on its fixed tame generator.
The auxiliary K, p-unit class number, ordinary/nonanomalous,
surjective/Manin range and all original conductor conditions remain.
The two tame coordinates are independent only under the stated
p-unit class-number hypothesis.

Write

    kappa=kappa_(ij,Q)^raw,
    tau_i=t_(i;j)=-a_j^+(eta_i),   tau_j=t_(j;i)=-a_i^+(eta_j).

The actual first lifts H_i in H1(U,N_i), H_j in H1(U,N_j) were
constructed there. In the clean range their old localizations are

    loc_i H_i=0,       loc_j H_i=tau_i beta_j,
    loc_j H_j=0,       loc_i H_j=tau_j beta_i.              (1)

The classes b_i,b_j are the specified transverse twist tests, not
arbitrarily chosen local sections. They are transverse beta at their
own place and finite elsewhere. Their cross coordinates satisfy

    G_ij=B_i(loc_i b_j,beta_i)=-G_ji.

All B_v use the compact-FIRST Weil convention. Their coordinates are
those of the completed point-dual transport, without an omitted unit.
Thus R_i(kappa)=tau_i G_ij and R_j(kappa)=tau_j G_ji.

Sections 2–3 give the actual global corner and its old local algebra.
For the explicit b_i corrections and the clean finite conditions in
Sections 4–7, additionally assume p-unit Tamagawa and Sel_n(E^D)=0,
as in the predecessor. Primitivity of kappa is imposed only when
explicitly stated. No finiteness of Sha(E), rational point for Kato,
or relation between complex and p-adic derivatives is assumed.
The fixed A,B,d0 are never changed.

## 2. A unique actual global corner

Put

    C=R[x,y]/(x^3,y^3),       J=xyC,
    u=xy, v=x^2y, w=xy^2, t=x^2y^2.                     (2)

Here x=log(1+Y_i)=Y_i-Y_i^2/2, and similarly for y.
The actual group-ring map sends sigma_i to exp(x), sigma_j to
exp(y). Both group orders are divisible by n, so this is an integral
quotient over R. Reflection negates x and y. The prescribed
inverse-Artin action is exp(a_i x+a_j y), with the reflection action
included on monomials. In J the degrees of u,v,w,t give coefficient
types M,M_D,M_D,M respectively.

Let mathfrak H be the actual Shapiro class of y_(ij) with the same
reflection-plus projection. Prime-to-p ring-class parts are traced by
their actual norms, without division by their degrees. Every pure
x coefficient vanishes modulo n by N_j y_(ij)=a_j(E)y_i; every pure
y coefficient vanishes by the i-norm. These include the two quadratic
axis coefficients. Hence its image in H1(U,M tensor C/J) is zero
after restriction to the actual ring-class field H_(ij).

The coefficient quotient is R-free and becomes constant apart from
M there. The established E(H_(ij))[p]=0 makes restriction injective
on this H1 and gives H0(U,M tensor C/J)=0. Consequently there is a
UNIQUE class

    H in H1(U,Q),       Q=M tensor J,                    (3)

mapping to mathfrak H. This is an actual preimage in cohomology,
not division by xy in an abstract free H1 module.

Projection modulo y^2 and modulo x^2 identifies its two first
projections with precisely H_i and H_j, by their earlier uniqueness.
On J, over K the coefficient action is

    u -> u+a_i v+a_j w+a_i a_j t,
    v -> v+a_j t,   w -> w+a_i t,   t -> t.

The epsilon twists in these formulas over Q are exactly those of
the displayed graded coefficient types. In common cocycle
representatives write H=(kappa,h_i,h_j,h). The equations are

    dh_i=-a_i cup kappa,       dh_j=-a_j cup kappa,
    dh=-a_i cup h_j-a_j cup h_i-a_ij cup kappa,
    a_ij(g)=a_i(g)a_j(g).                                (4)

Here da_ij=-(a_i cup a_j+a_j cup a_i), so the last target is closed.
Equation(3) supplies its actual primitive h. This proves the vanishing
of the mixed GLOBAL obstruction for these particular raw first lifts.
It says nothing yet about native point images.

Before the reflection projector, the restriction of the top
coefficient to H_(ij) is the Kummer class of the actual point moment

    Q_ij^[2,2]=(1/4) sum_g j_i(g)^2 j_j(g)^2 g y_(ij)
                         in E(H_(ij)) tensor Z_(p).       (5)

The v,w coefficients have the negative weights
-j_i(g)^2 j_j(g)/2 and -j_i(g)j_j(g)^2/2. These signs follow from
the inverse-Artin exponential; the u coefficient has positive
j_i(g)j_j(g). All fixed transversals and unnormalized traces remain.
Changing an integer Artin representative changes these finite
classes by an n-multiple. Extraction of (5) is only asserted over
H_(ij), where the coordinates become constant; it is not declared
an equivariant global projection or a rational point.

The point input is [Howard1202.6340v1, §1.7, equation(13) and
Lemma1.7.2](https://arxiv.org/html/1202.6340v1#S1.SS7),
archive dated 28 February 2012. Only the actual norm, reduction and
raw cocycle construction are used, not the unqualified global
coefficient-automorphism wording in1.7.5.

## 3. Full old-local H0 and the half-quadratic term

The native point image in Q is zero at BOTH old primes. Indeed Q is
a quotient of the actual regular Shapiro coefficient via multiplication
by xy. Its inertia norm in either variable has zero constant and
linear terms modulo n. The same argument for C retains the quadratic
term d(d-1)(2d-1)/12; all three coefficients are zero because n|d
and p>=5. Thus the actual point class in C is zero locally and
loc(H) belongs to the boundary from H0(C/J).

Work in the fixed old Frobenius gauges. The other Artin coordinate
is zero locally. With E's Frobenius eigenvectors e_+,e_-, the
quotient C/J has basis 1,x,x^2,y,y^2.
At i its inertia invariants are x^2,y,y^2; Frobenius selects

    x^2 e_+,       y e_-,       y^2 e_+.

The first lifts invariantly to C. The boundaries of the other two
on the fixed inertia generator are exactly

    delta(y e_-)=(-u+v/2)e_-,
    delta(y^2 e_+)=(-w+t/2)e_+.                          (6)

Bottom strictness removes the first coefficient, since its image
in the bottom singular line is primitive. The second is fixed by
the projection to H_j and (1). In locally normalized cocycles,

    loc_i H:
      kappa=0, h_i=0, h_j=tau_j beta_i,
      h=-(tau_j/2)a_i^2 e_+,
    loc_j H:
      kappa=0, h_j=0, h_i=tau_i beta_j,
      h=-(tau_i/2)a_j^2 e_+.                            (7)

Here beta_i=-a_i e_+ as an M_D cocycle. The top expression is a
cochain, not a class in H1(M): its differential is precisely the
localized mixed target in(4). Formula(6) proves that it cannot be
discarded by assuming the full local restriction injective.
At j the calculation is obtained by exchanging x,y and v,w.

There is a further kernel relevant to top corrections. In Q/Mt
the i-inertia invariants are v,w, and its H0 vectors are
v e_-,w e_-. The first lifts to Q. The second has top boundary
a_i e_- and spans the transverse E-line. Therefore

    ker[H1(Q_i,Mt)->H1(Q_i,Q)]=H_tr^1(Q_i,M).             (8)

The same holds at j. Away from i,j in the clean range, the preimage
of the native Q point image on Mt is the finite E condition. This
follows from the R-split inertia quotient, and at p from the ordinary
plus sequence with no nonanomalous quotient invariants, exactly as
in the checked predecessor. Denote these top correction groups by

    J_top,v=H_tr^1(Q_v,M) at v=i,j;  H_f^1(Q_v,M) elsewhere. (9)

These groups describe the actual point-image preimage. They are not
replaced by arbitrary finite conditions at the ramified old places.

## 4. The particular self-cup classes and their exact pairing

For each r=i,j put

    A_r=a_r cup b_r in Z^2(U,M).                         (10)

At r it has the explicit primitive

    gamma_r,r=(1/2)a_r^2 e_+,
    d gamma_r,r=a_r cup beta_r,                          (11)

since d(a_r^2)=-2 a_r cup a_r in the twisted scalar convention.
At the other old prime a_r=0, so choose gamma=0 there. At the other
places choose a primitive in the finite local complex: unramified
cohomology has no H2 away from p; at p the ordinary plus H2 is zero
by the nonanomalous dual invariant calculation. At D the odd-p
quadratic inertia makes the relevant twisted classes trivial.
Real Tate terms vanish for odd p.

These local choices define an actual compact class

    g_r=[(A_r,(gamma_r,v)_v)] in
    D_top=H_c^2(U,M)/partial^+(direct-sum_v J_top,v).      (12)

The cone uses D(c,b)=(dc,res c-db), partial^+(b)=(0,+b).
Local changes within the finite complexes change (12) by the
specified finite boundary. At the old places use the fixed
beta-normalization in(11). If a cohomologous b_r representative
is put into that normal form, transport gamma by the same
cochain homotopy. Ambiguity of its normalizing zero-cochain is a
local invariant; its a_r-boundary is precisely the transverse line
in(8). Thus (12) is well-defined in the displayed quotient.

**[NEW] Proposition4.1.** In the original compact-FIRST convention,

    <g_i,kappa>=-R_i(kappa)/n,
    <g_j,kappa>=-R_j(kappa)/n.                            (13)

The pairing is in n^-1 Z/Z; no unrecorded Q/K factor is inserted.

*Proof.* Use the actual h_r from(4). Define the pointwise cochain
c_r(g)=a_r(g)b_r(g), with values in M. Direct substitution into
the inhomogeneous differential gives

    d c_r=-(a_r cup b_r+b_r cup a_r).

With Weil evaluation understood in the stated coefficient order,

    U_r=-b_r cup h_r-c_r cup kappa
    satisfies dU_r=(a_r cup b_r) cup kappa.              (14)

This is an explicit cup homotopy; it is not an informal interchange
of degree-one factors. The compact pairing is therefore

    sum_v inv_v[
       b_r,v cup h_r,v+(gamma_r,v+c_r,v) cup kappa_v].     (15)

At an old place strict kappa can be represented by zero. This local
replacement does not change (15): if kappa is replaced by
kappa-dq and h_r by h_r-a_r cup q, the difference is
d[(gamma_r+c_r) cup q]. Thus it suffices to use the normalized
localizations(1). At r, h_r is zero in N_r; its possible top
representative lies in the transverse kernel and pairs to zero
with beta_r. At the other old s, h_r=tau_r beta_s and b_r is finite.
The contribution is tau_r G_sr/n=-tau_r G_rs/n.
All other local contributions are zero in the finite complexes;
at p the plus-coefficient pairing is zero. This proves(13).
The finite G coordinates are the fixed ones. In K finite Frobenius
coordinates they include the existing factor2; tame inertia and
the rational compact trace receive no extra factor. QED.

## 5. Correcting the actual first lifts

First work with a scalar alpha in R multiplying the ACTUAL kappa.
When kappa is primitive, the fixed corrected Kato input is exactly
z=alpha_z kappa; alpha_z retains its full d0 and fixed A,B. It is
not required to be a unit. The following construction uses no
division by alpha.

All clean native first lifts, if they exist, have the form

    F_i=alpha H_i-alpha tau_i b_j+lambda_i b_i,
    F_j=alpha H_j-alpha tau_j b_i+lambda_j b_j,            (16)

in their respective top M_D slots. Their own native conditions are

    alpha tau_i G_ij=0,       alpha tau_j G_ji=0.          (17)

This is the completed first-stage obstruction, not a new conclusion.
At the OTHER old place their finite values are respectively
lambda_i G_ji and lambda_j G_ij. A native Q lift is zero at both old
places, so a necessary further projection condition is

    lambda_i G_ji=0,          lambda_j G_ij=0.             (18)

These are annihilator conditions in R, not licenses to divide G.

Subtracting the first corrections in(16) from the actual H changes
the mixed equation by the PARTICULAR two-cocycle

    A(alpha,lambda)=
       alpha tau_j a_i cup b_i+alpha tau_i a_j cup b_j
          -lambda_j a_i cup b_j-lambda_i a_j cup b_i.      (19)

Thus a global top adjustment v must satisfy dv=A(alpha,lambda).
This is the exact remaining global class on the actual objects.
The uncanceled fixed terms are the self-cups in(10), not an
arbitrary H2 class or an adjustable old-local section.

Under(18), each lambda_s a_r cup b_s, r!=s, has zero old local
cocycle in the normalized finite frame. At the other places it has
a finite primitive as before. Let g_(r,s;lambda_s) be the compact
class with these primitives and with zero old primitive. Then the
actual compact mixed correction is

    g(alpha,lambda)=
       alpha tau_j g_i+alpha tau_i g_j
         -g_(i,j;lambda_j)-g_(j,i;lambda_i) in D_top.      (20)

At i its prescribed primitive is
alpha tau_j a_i^2 e_+/2, canceling the actual top cochain in(7);
likewise at j. Equations(17),(18) kill the remaining lower local
terms. Therefore g(alpha,lambda)=0 is SUFFICIENT and NECESSARY
for a top adjustment with the actual native images, for these
particular first lifts. This follows by the cone boundary equation:
v differs from the stated local primitives by precisely(9).

The same proof as(14)–(15), using c(g)=a_r(g)lambda_s b_s(g), gives

    <g_(i,j;lambda_j),kappa>=0,
    <g_(j,i;lambda_i),kappa>=0.                          (21)

Indeed at r the finite term lambda_s b_s is zero by(18);
at s both b_s and the raw h_r=tau_r beta_s are transverse,
so their pairing is zero. Finite conditions give zero elsewhere.
Combining(13), (20), (21) and G_ji=-G_ij gives the actual identity

    <g(alpha,lambda),kappa>
       =-alpha(tau_j tau_i G_ij+tau_i tau_j G_ji)/n=0.    (22)

Neither tau_i, tau_j nor G is asserted zero in this calculation.

## 6. Primitive clean gluing and its surviving ambiguity

**[NEW] Theorem6.1.** Suppose kappa has exact order n, in the clean
range above. For the first lifts(16), a native Q mixed lift exists
if and only if(17) and(18) hold. There is no further compact mixed
obstruction for these lifts. For the actual input take alpha=alpha_z.

*Proof.* The completed higher-residue theorem gives
Sel_n(E/Q)=R^3, strict group S=R kappa, and surjectivity of the
two old finite localizations onto R^2. The finite Poitou–Tate
sequence then makes the group relaxed at both old places equal
to the classical Selmer group: its possible singular quotient
annihilates the surjective old localization and is zero.
Intersecting the two transverse conditions with the finite
conditions gives precisely the strict group S.

Each group(9) is self-annihilating for the Weil pairing.
Finite compact duality therefore identifies D_top perfectly with
the dual of this two-transverse E Selmer group, namely (R kappa)^*.
Equation(22) says that g(alpha,lambda) pairs to zero with its
actual generator, and hence is zero. The cone equation supplies v
with the local primitives specified in Section5. Adding that top
adjustment to alpha H and the displayed first corrections gives
the required Q-class. Necessity of(17),(18) follows from its two
projections and the zero native image at the old places. QED.

The construction is finite-coefficient: choose the actual global
cocycle data and solve dv=A together with its local equations,
allowing exactly(9). The theorem proves existence of a solution;
it does not make a chosen finite linear-system solution motivic.
No nonunit is canceled. If G is a unit, (18) forces lambda_i=lambda_j=0.
If G is a nonunit, keep its full annihilator ideal.

For fixed first classes, the set of native mixed lifts is a torsor
under R kappa in the top M slot. To see the exact scope, put
P=Q/Mt. The map H1(U,P) to the pair of first H1 classes with the
same bottom is injective, by H0(U,M)=0. Also H0(U,P)=0, so the
top H1(U,M)->H1(U,Q) is injective. Local differences of native
top lifts have precisely the conditions(9), whose global group
was proved to be R kappa. This proves the torsor assertion.

The ambiguity is realized arithmetically: multiplication by xy
of the original Shapiro class maps to t kappa in Q. It is a
quotient of the original point Kummer class, so it preserves the
native images and every lower coefficient. In the primitive case
it has exact order n. Thus the new corner does not select the
top coefficient even after the mixed equation has been solved.

## 7. Nonprimitive scope, primary inputs, and the remaining zero

Without primitivity, (3)–(7) still give the actual global corner and
its old local cochains. In the clean range(10)–(22) still compute
the compact correction and its pairing with this kappa whenever
the displayed first and cross-local conditions hold. The dual
two-transverse group is not then asserted cyclic. The exact
remaining mixed class is

    g(alpha,lambda) in D_top,    <g(alpha,lambda),kappa>=0, (23)

and its vanishing has not been proved from that one test.
An arbitrary nonprimitive cofactor z is not declared alpha kappa.
Its earlier fixed-class integral defects and scaled lifts remain
exactly those of the predecessor.

Outside the clean range, retain the actual native image W_Q,v and
its top preimage
J_top,v={c in H1(Q_v,M): image(c) in W_Q,v}.
The coefficient corner and its old good-place H0 calculation do
not erase these bad-local groups. The b_i inverse and finite
substitutions used in Sections4–6 are not asserted in that range.
The general mixed equation is still(4) with the actual permitted
first corrections, and its compact obstruction is obtained from
those native local lifts, as in the completed tame proof.

Primary duality input checked directly:
[Milne, Arithmetic Duality Theorems, second edition, I.4.10 and II.2.9](https://www.jmilne.org/math/Books/ADTnot.pdf)
for finite Poitou–Tate and the Galois/etale comparison on this
p-inverted affine U; and
[Demarche–Harari1804.03941v3, Theorem1.1 and Proposition2.1](https://arxiv.org/html/1804.03941v3)
for finite compact duality and its localization cone. The primary
pairing is compact first. Real Tate modifications are zero at odd p.
The exact point norms and source version are recorded in Section2.
No fresh Selmer finiteness premise is inserted into these inputs.

The positive new result is compatible mixed gluing for the PARTICULAR
corrected first lifts in the primitive clean case, not first-stage
annihilation. The products alpha_z tau_i G_ij and alpha_z tau_j G_ji
in(17) remain the first exact unresolved conditions for the actual
cofactor. The arithmetic moment(5) and the computed self-cup
pairings have not been identified with L'''(E,1).

**[OPEN, MQ-TP5].** Use the extra COMPLEX zero to annihilate those
particular first products, control(23) in the nonprimitive/nonclean
cases, and select the subsequent top class through an actual
arithmetic comparison. The construction supplies no finite-Sha or
rational Kato-point premise and does not identify a p-adic derivative
with that complex derivative. Full BSD is not proved or disproved.

All new deductions, including the compact-first cochain identity,
native top preimage and primitive gluing theorem, passed the separate
review without a formula change. No old numerical script or certificate,
shared predecessor edit, or new subagent was used.
