# Research state — read this first after context loss

Checkpoint date: 2026-09-13. Maintainer: root/coordinator.
The original [fresh-context audit](cold-resume-review.md), the
[construction-round audit](continuity-round-audit.md), and the newest
[Bott/derived-unit recovery audit](continuity-bott-derived-audit.md) passed.
Continuity setup is complete. Ledger24's four core constructions, two
additional constructions and the separate local Taylor certificate all
passed independent review. The arithmetic reproduction and its full audit
are saved. A temporary capacity error was resolved on the SAME Astra/xhigh
handle without rerunning arithmetic. No full BSD proof or counterexample
exists. The current checkpoint below is authoritative over the historical
ledger23 inputs retained in §5. ROUND25 is now complete with four
independent PASS reviews. The current override immediately below
supersedes the historical completed-round tables.
This file is operational memory. The full proofs are linked below.
`final-report.md` is the mathematical status of record; the ledger records
which approaches have already been tested.

### Fresh-computer migration override — 2026-09-13

The user requested a migration/readiness audit before more mathematics.
No round 26 has started. [START_HERE.md](../../START_HERE.md),
[the fresh-session prompt](fresh-session-prompt.md), and
[the migration guide](migration-guide.md) now provide recovery without
the original conversation. See [the migration audit](migration-audit-2026-09-13.md)
for scope, evidence and portability limits.

Old task/agent IDs and goal/usage snapshots below belong to the original
session. On a different host, inspect its actual tools; a missing old goal,
agent handle or Git directory is normal. The mathematical objective and
round25 proof/review boundaries persist. The new coordinator may be Astra
or an equally capable tooling AI as the user allowed; research subagents
retain the requested Astra/xhigh setting. No current platform state is
inferred from the historical usage snapshots.

Run `python3 compute/scripts/verify_handoff.py` in an extracted snapshot
before editing. It checks saved bytes and dependencies, not mathematics.
The archive preserves staged/uncommitted research, raw outputs and selected
ignored caches. Native Sage/PARI binaries are excluded and must be rebuilt
when needed. Exact old `/tmp/` references are relocated in
[the source index](source-cache-index.json). The original CM verifier is
unchanged; a separate portable adapter records its two path substitutions.
Read the completed-round override below, then select a target only when
the user resumes research here or invokes the fresh-session starting prompt.

### Completed round25 override — save/read this first

All four constructions and complete independent reviews are saved. Root
read them fully. The round made mathematical progress. No new numerical
calculation or old certificate rerun was needed, and no process is live.

| Result | Proof / separate verdict |
|---|---|
| Actual tangential Poisson source; weighted trace removes cusp constant | [Proof](poisson-tangential-fiber-attack.md), [PASS](review-poisson-tangential-fiber.md), [restart](poisson-tangential-fiber-checkpoint.md) |
| Exact derived/Gersten point–K2 models; explicit first difference and filled second difference | [Proof](point-k2-derived-symbol-attack.md), [PASS](review-point-k2-derived-symbol.md), [restart](point-k2-derived-symbol-checkpoint.md) |
| Nonzero global CM coefficient classes/deep lifts, surviving every common point correction | [Proof](cm-global-selector-attack.md), [PASS](review-cm-global-selector.md), [restart](cm-global-selector-checkpoint.md) |
| Actual biquadratic Heegner corner; conditional native mixed gluing | [Proof](heegner-mixed-quadratic-attack.md), [PASS](review-heegner-mixed-quadratic.md), [restart](heegner-mixed-quadratic-checkpoint.md) |

The tangential reviewer reported a usage limit AFTER saving its full
280-line PASS. Root verified the file and completed only editorial
proof/checkpoint links. No result or review was lost and no model was
changed. Latest get_goal status is usageLimited; the separate fresh account
tool reports ordinaryUsageAllowed=true. No reset credit was consumed.
Do not mark this unfinished objective complete or mathematically blocked.
Revalidate both states before a new dispatch. The last agent inventory
listed only odd_rank_bridge as completed; the other handles were absent.
Do not treat historical ownership as a live handle or silently change models.

Next mathematical actions are in [the updated plan](next-research-plan.md).
The first point–K2 difference, first Heegner native products and top
selection, secondary CM-to-Kato/Selmer operation, rational arithmetic
comparison and full BSD remain open. The global CM result has ENLARGED
coefficients and zero augmentation; it is not ordinary Selmer nonvanishing.
The weighted Poisson source has hSigma=-388cpi qF exactly, but its real
Deligne/Petersson operation is not thereby rational. No next construction
has been dispatched. The full objective and Astra/xhigh requirement persist.

### Completed round 24 checkpoint — 2026-09-13

The goal tool was re-read after compaction and remains active, with no
specified budget. Full BSD is unresolved. The root has recovered the
prior proof files and live agent state; no numerical process is running.
The final manifest records the exact saved artifacts and dependency
checks. Revalidate tool state on resumption. The last two goal turns
made mathematical progress, not a repeated external blocker.

| Owner (research agents remain Astra/xhigh) | Current construction / restart |
|---|---|
| higher_period_integrality | COMPLETE: [actual Poisson source motive](poisson-path-motive-attack.md) passed [root's audit](review-poisson-path-motive.md), final editorial proof `a6470b0f45c1d33fef2b8581f0d4af810410594abda4573134982548e322127f`. Its [local Taylor arithmetic PASS](review-cm-local-taylor-certificate.md) includes a different implementation and the sharper P-or-Q consequence. [Verifier](../../compute/scripts/verify_cm_local_taylor_mod5.py)/[output](../../compute/data/cm_local_taylor_mod5_independent.json) are durable. Capacity retry is resolved; no task remains on this handle. |
| odd_rank_bridge | Asymmetric CM proof and [cofactor audit](review-heegner-cofactor-lift.md) COMPLETE. The latter passed at repaired `1069acaa79900311139625292de131d1e4d380f768f6d9464cd43d42c995150d`. Root read the full audit and directly checked Kim's same-index positivity. Revalidate before reuse. |
| uniform_witness | Completed [CM PASS](review-cm-asymmetric-ray.md) and [core local-map PASS](review-cm-local-point-comparison.md) at repaired five-section revision `8fad3936741f7be8ff173ffb91c5da99036e94a55545eec685a21c56c975b5b8`. The later numerical §6 is explicitly outside that verdict. Revalidate before reuse. |
| root | COMPLETE: [exact local point-map note](cm-local-point-comparison-attack.md), final proof `79d4ec292b1b2094e7fc388c4c40efff825a34bd8da7243bc2eeab5b8ed9ade6`; [checkpoint](cm-local-point-comparison-checkpoint.md). Core and numerical PASS reviews read, sharper consequence added from its separately reviewed proof. All six construction results are integrated. No root process is live. |

Next root action: read [the new plan](next-research-plan.md), select and
dispatch the next bounded attack on its actual arithmetic targets, with
the same Astra/xhigh handles. Do not repeat the completed source motive,
local coefficient calculation or failed linear comparison. The new local
C2 is a5-unit; its exact nonadditivity proves that no scalar matches this
family at64P,64Q,64(P+Q). At least one of the two P,Q local classes is
nonzero mod25. This is NOT the old cyclotomicc2 and does not give global
Selmer descent. The point–K2 remaining component, Heegner particular
vanishing, rational arithmetic comparison and full BSD are still open.

## 1. Objective and completion test

The active user goal is:

> you do not stop until this this proved or disproved. you are free to use all my tokens, spinup agents, think and research

“This” means **full Birch–Swinnerton-Dyer for every elliptic curve over Q**,
including rank equality, finiteness of Sha, and the precise leading-term formula
in `../00-charter.md`. A genuine counterexample would also finish the goal.

**Current status: neither a full proof nor a counterexample has been obtained.**
Do not mark the goal complete from any result below. A proof of a test curve
would still leave the universal objective. The goal is active and has no
specified token budget. Its current tool state must be re-read on resumption.

The user expressly requires all research agents to be **GPT-6 Astra with
extra-high reasoning**: tool values `model="gpt-6-astra"`,
`reasoning_effort="xhigh"`. The user also expressly requested durable notes
before further research so work survives context compaction.

## 2. Immediate recovery procedure

1. Read this file, [the ledger](research-ledger.md), and [the current report](final-report.md).
2. Inspect the current active goal, `git status --short`, and `collaboration.list_agents`.
   The coordinator's goal tool is authoritative for the parent task. During the
   cold-resume audit a subagent saw `goal=null` while the root tool confirmed
   this goal active; a subagent must not treat that as cancellation or create
   a duplicate goal. Parent task ID: `01a09445-0094-7400-a38c-2c594729b18d`.
3. Read the actual proof/checkpoint file of any agent to be resumed. Reuse a
   verified existing agent when available; otherwise spawn a replacement on
   GPT-6 Astra/xhigh with its task and the file paths, not a reconstructed transcript.
4. Preserve the uncommitted worktree. Check
   [`checkpoint-manifest.json`](checkpoint-manifest.json) for artifact hashes.
   A changed hash means inspect the new file; it does not automatically mean damage.
5. Resume the concrete next action in the CURRENT ROUND OVERRIDE above.
   Section5 below retains historical inputs and conventions, not live ownership.
   Update this checkpoint when the next action changes.

No permission question is needed merely to resume authorized research.
Research may continue once the requested continuity checkpoint is verified.

## 3. Established results and their evidence

These are project results, not claims of historical novelty.

| Result | Authoritative proof / verification |
|---|---|
| Algebraic and analytic ranks are exactly 2 for 389a1 and 3 for 5077a1 | [Analytic certificates](analytic-rank-certificates.md), [independent review](review-analytic-certificates.md) |
| For 389a1 at 5, the weighted modular-symbol sum at 41·61 is exactly 244, hence delta=4 mod 5; Sha[5-infinity]=0 | [Initial continuation, §§1–2](continuation-2026-09-12.md) |
| Sha primary parts at 2, 3, and 389 for 389a1 are zero | [Exceptional primes](exceptional-prime-finiteness.md), coordinator-reviewed image and valuation proof |
| Full Mordell–Weil basis P=(-1,1), Q=(0,-1); raw descent points (-2,0),(4,8) have index 3 | [Archimedean certificate, §3](bsd-archimedean-bound.md) |
| The real BSD quotient of 389a1 is between 533217021/536870912 and 541003931/536870912, hence between 0.9931 and 1.0077 | [Archimedean proof](bsd-archimedean-bound.md); coordinator reviewed and reran at another doubling depth |
| Odd analytic rank at least 3 forces Selmer corank at least 3 under the stated ordinary irreducible hypotheses, and in the semistable supersingular case | [Odd-rank bridge](odd-rank-selmer-bridge.md) |
| Under Kim's hypotheses and M-infinity=0, the first unit Kurihara index equals dim Sel_p = rank + dim Sha[p] | [Uniform-witness proof](uniform-witness-attack.md), [independent review](review-uniform-witness.md) |
| For 389a1, residual images are surjective at every good p>=5; Castella–Sano supplies a unit witness of some index at every p outside {2,3,389} | [Uniform-witness proof, §§1–3](uniform-witness-attack.md) |
| Integral determinant/cofactor lemmas, a valuation-only sufficient BSD criterion, and precise countermodels to interpolation shortcuts | [Derived-comparison attack](derived-comparison-attack.md), coordinator review recorded there |
| Uniform divisor/splitting-field degree bounds for locally soluble genus-one torsors are equivalent to full Sha finiteness | [Geometric attack](genus-one-finiteness-attack.md), selected propositions coordinator-reviewed |
| For E:y²=x³+39x, c2=20 mod25 proves an actual derived class nonzero and Sha[5-infinity]=0 | [CM certificate/proof](cm-derivative-nonvanishing-attack.md), [root review](review-cm-derivative-nonvanishing.md), [third integral audit](review-cm-derivative-integral-bound.md) |

The odd-rank result resolves the former GAP 5 in the inherited obstruction
document's ordinary range. It does not prove rank 3 BSD or the next lower
bound 5. It gives full-I_n vanishing of Kurihara numbers with fewer than
three factors under the additional surjectivity/Manin hypotheses.

### Established results from the later rounds

The family, modular visibility, higher-period, and coherent-moment notes are
complete with reviewed elementary deductions. Their outcomes are summarized
in [ledger §9](research-ledger.md#9-family-visibility-and-higher-period-round--completed-work)
and the current report. In particular, the actual eta correction for 389a1 is
rigorously between 110 and 111, so omitting it is disproved. The entire possible
remaining Sha(389a1) is invisible in J0(389), not proved zero. None of these
results settles full BSD or leading-term rationality.

The subsequent arithmetic-surface/Green/Brauer round has PASS reviews:
[Weil–étale](review-weil-etale-lattice.md),
[Hecke–Brauer](review-hecke-brauer.md), and
[arithmetic Green](review-arithmetic-green.md). The actual model's Picard
group is Z³ and its full Brauer group equals Sha(389a1), including at 2.
Required degree-three finite generation remains exactly full Sha finiteness.
The eta Hodge class and its arithmetic theta pairing are constructed, but
its degree-zero elliptic point projection is zero. No annihilator is proved.

For the CM curve y²=x³+39x, algebraic and analytic ranks are now certified
equal to two. The new [data](../../compute/data/cm39_analytic_rank_certificate.json)
uses the same proved Mellin algorithm at 112 bits/T=48, strictly inside the
agent's earlier 96-bit/T=40 interval. Root reconstructed all coefficients
through 1405 from point counts and Hecke recurrences. The theta/regulator
identities and restricted moment normalization also passed a separate
[source and deduction review](review-cm-regulator-tensor.md). Rationality
and the moment-to-regulator arithmetic identity remain unproved.

The preceding explicit-construction round is complete and reviewed; see
[ledger §11](research-ledger.md#11-explicit-relative-motives-and-twisted-bundles).
Actual relative 1-motives now realize the full height determinants, including
fixed rational finite-place corrections, for 389a1 and the CM curve.
The ordinary 389 model has π₁=1, CH₀=0 and K₀=Z⁴, but its c₁ cokernel
is still Sha[n]. For every period-n Brauer class, an actual rank-n twisted
bundle and degree-n splitting object are constructed. Its Fourier–Mukai
line lies on the corresponding torsor; the actual generic twisted K₀ is
Z⁴ with numerical Euler determinant n². These are not uniform period bounds.

The subsequent Bott/derived-unit/spectral/connection round also passed
review; see [ledger §12](research-ledger.md#12-bott-derived-units-spectral-jets-and-projective-connections).
It constructs actual finite-coefficient K-theory lifts, an integral
CM derived-unit class with an exact full-Selmer height identity, a
full-curve spectral jet, and integral local degree-zero connections.
The remaining global rational comparison and uniform Sha bound are
explicit in §5. The subsequent constructions are reviewed through ledger §23. The
fresh-prime detection now works under the original irreducible O5
hypotheses, with actual signed Gysin classes and a global residue
presentation. The obstruction is also the reduction of an explicit
trace-zero cofactor point. Two actual CM ray derivatives specialize
exactly to the old cyclotomic derivative and now individually satisfy
the integral Selmer conditions. The full integrated theta source is
constructed analytically; its noncentral K2 divisor now has a unique
rational arithmetic lift and a sufficient integral multiple. Actual
higher Heegner residues and arithmetic theta components are tested. Heegner vanishing and the rational
global BSD comparison remain open.

## 4. Exact unresolved mathematical targets

### Universal objective

We still need the general analytic/arithmetic rank comparison, full Sha
finiteness, and the leading coefficient, including CM curves and all primes.
Do not confuse the following test-curve targets with this universal scope.

### Test curve 389a1

Let S0={2,3,389}. For every p outside S0, a unit Kurihara family already
exists, and its first unit index satisfies

    u_p = 2 + dim_Fp Sha(389a1)[p].

**PrimeIndex-389:** prove u_p=2 for EVERY p outside S0. The completed S0
calculations would then imply Sha(389a1)=0. An almost-all-prime theorem
introducing additional exceptions must handle those extra primes separately.

**Leading-389:** prove the complex BSD quotient n_E equals 1. The certified
interval makes integrality sufficient. Rationality alone is not sufficient.
Neither rationality nor the required global complex/arithmetic comparison
has been proved.

Alternative targets, with explicit full hypotheses in their notes:

- A fixed integer matrix of rational corank 2 whose mod-p cokernel surjects
  onto the entire Sel_p would give a finite-prime bound from a nonzero minor.
  No such matrix/maps have been constructed. Mapping only to rational points
  is insufficient.
- The one-sided leading-term criterion requires rationality, primary finiteness
  at every prime, and the complex valuation inequality at every prime. The
  archimedean inequality is completed for 389a1; those other premises are not.
- A uniform degree bound for closed points on all locally soluble 389a1 torsors
  would give full Sha finiteness. Fixed-degree finiteness and period=index do
  not supply this uniform bound.
- O5: in odd analytic rank at least 5, prove vanishing of every two-prime
  Heegner Kolyvagin class in the rank-zero auxiliary twist setup. The exact
  equivalence to Selmer corank at least 5 is in the odd-rank note.

## 5. Historical ledger23 inputs and retained conventions

The four constructions in [ledger §23](research-ledger.md#23-finite-poisson-periods-motivic-coefficients-and-integral-lifting-tests)
are COMPLETE, with separate PASS reviews and root inspection.
The marked-family proof has a separate additional verdict for its
motivic coefficient object and real lift. The goal turn made
mathematical progress; full universal BSD remains active and unresolved,
with no token budget or repeated external blocker.
That completed-round snapshot predates the current override above.

### Historical ownership and restart routes

All three existing research handles remain GPT-6 Astra/xhigh and
reported completion. Revalidate the actual tools before reuse.

| Agent | Completed work and restart |
|---|---|
| /root/odd_rank_bridge | CM coefficient cone/fixed-jet theorem; marked-family review including §§9–10. [Restart](cm-derived-unsmoothing-checkpoint.md). |
| /root/uniform_witness | Reflection/Prym norm lattice; Poisson iterated-source review. [Restart](heegner-prym-norm-checkpoint.md). |
| /root/higher_period_integrality | Moving marked/motivic coefficient family and real lift; CM review. [Restart](marked-coefficient-extension-checkpoint.md). |
| Root/coordinator | Finite Poisson iterated-period formula; Prym review; proposed/reconstructed the two additional marked steps and inspected all proofs/reviews. [Restart](poisson-iterated-source-checkpoint.md). |

No root process, download or numerical certificate is live.
Only NEW exact arithmetic was checked: the four h_j(S),h_j(T)
fractions and residue matrix, and v_a(L_a)=-12 for the two saved
CM ratios. Root's initial ad hoc h2 checker omitted its denominator3;
correcting the checker to the actual formula made all values match.
The author table required no change. No old certificate, psi-value
reproduction or prime scan was rerun. Preserve the uncommitted tree.
The completed continuity check covers223 artifact hashes,852 local
artifact links,nine current certificate JSONs and all six dependency
hashes. The legacy certify_ran.json remains two valid JSONL records.

### Predecessor proof and independent review routes

| Construction | Proof and review |
|---|---|
| Finite exact Poisson iterated-period representation | [Proof](poisson-iterated-source-attack.md), [PASS](review-poisson-iterated-source.md) |
| Moving marked family, actual motivic coefficients and real lift | [Proof](marked-coefficient-extension-attack.md), [PASS including additions](review-marked-coefficient-extension.md) |
| Reflection-field Prym lattice and integral lifting class | [Proof](heegner-prym-norm-attack.md), [root PASS](review-heegner-prym-norm.md) |
| Actual CM cone class and all fixed point jets | [Proof](cm-derived-unsmoothing-attack.md), [PASS](review-cm-derived-unsmoothing.md) |

### Exact new results and corrections to preserve

- **Poisson periods.** Use the actual reduced fiber u=2 and all
  conjugate basepoints. The length-two relative pair has initial
  endpoint, diagonal and terminal endpoint; when endpoints agree,
  separate the constant line. EARLIEST-FIRST order is fixed by
  dI_(eta,xi)=A_eta xi. With rho having conjugate alpha periods,
  Phi=Re[2lA-I_(alpha,theta)-I_(rho,theta)] has curvature
  4pi c_pi ReF dmu and an explicit CONSTANT real monodromy.
  Finite RR principal parts and the FULL-genus real period matrix
  remove it. The cusp remainder after subtracting the principal
  primitive is single-valued, with only a(q)log|q|², not arg(q).
  Prior q_F existence proves compatibility but no q_F value
  chooses the RR correction. Finally
  q_F=(U_b-U_b(infinity))/(4pi c_pi), with all finite parts and
  period products retained. This is a finite real formula from
  genuine geometric period data, not a rational motivic morphism.
- **Marked family.** Translate the FULL corrected Z_j on E while
  retaining A={O,P,Q}. Rational Miller/h_j ratios give a_ij(x),
  with div a_ij=sum n_(j,Z)([P_i-Z]-[-Z]) and a_ij(O)=1.
  At x=-R the residue matrix is[[4,3],[3,4]], determinant7.
  The algebraic connection is nabla e_j=-sum dlog a_ij t_i.
  Archimedean H(x)=H+log|a(x)|; its added finite v_p(a)logp
  terms keep GLOBAL H fixed by product formula. The moving
  archimedean determinant is not a new global regulator.
  Both original cusps recover M0. New Sigma lies in the interior.
  Over -R, exterior log-square is14e² and T-1 bottom is7e².
  No invariant top lift exists; the derived boundary has
  J3 plus4J2 plus4J1, with H0/H1 dimensions9.
- **Motivic coefficient object DONE.** In DM_B(S), construct
  C_rel=Fib(p_*1→q_*1), A_B=Cofib(1→q_*1),
  V_all=(C_rel[1])^vee, and the marked homotopy pullback
  V_mot=Fib(V_all plusL→A_B^vee). The positive divisor
  marking follows from beta→(0,-beta) paired with(gamma,-Z).
  Puncture localization retains(-1)[-2] and proves dualizability
  of these specific objects. The Artin trace projector divides
  by the full support degree b. The rational antisymmetrizer
  divides by2; frame e1⊗e2-e2⊗e1 maps to1 on source/target,
  whereas the raw projector is half that generator.
  The shift matches the earlier PLUS geometric interchange.
  W_mot and its actual top map/fiber now give a genuine
  motivic coefficient triangle. No motivic t-structure or
  rigidity of all constructible motives is assumed.
  CD2019 and Tubach2407.02256v3 apply geometrically after
  base change toC; no arithmetic-MHM conjecture is inserted.
- **Real coefficient lift DONE.** S=C minus nonemptySigma,
  relative to the two old cusps, has H0=0 and Hi=0 fori>=2
  with ANY local system. Thus H1(W_C)→H1(C) is surjective;
  Kunneth with the testE gives the H2 product surjection.
  The original proper class is a_D(w), by the prior Ext theorem.
  Restrictw, lift it complex-cohomologically, and apply natural
  a_D to prove REAL O_W(c_S)=0. This uses the proper-origin
  representative, not just Betti-zero shorthand. It gives no
  canonical or motivic lift. The rational input, motivic
  degree-four obstruction/selection, and Sigma support
  comparison remain open. At s over -R the Poincare fiber
  corresponds to the nontorsion point -R in Pic0(E); it cannot be zero-trivialized.
- **Prym.** Restriction/norm for the reflection field have composite2,
  preserving the SAME marked CTP=-R_i/p^k. The smaller norm is
  UNHALVED and its characteristic-zero representation has no
  E^D factor. The actual Tate coefficient lattice is
  Lambda_C=Lambda+(1/n)Lambda0, n=p^k. Its H1=Z/n is generated
  byepsilon_C=(g e_H-e_H)/n, of exact ordern. The marked map
  has no invariant ONE-p-step lift; full torsion image/disjointness
  and matrix centralizers exclude EVERY equivariant coefficient
  lift. This does not prove that a particular class fails to lift.
  Its global cochain isepsilon_C(g)⊗g tilde z(h)+e_H⊗dtilde z/n.
  Keep the fixed A,B Bockstein defects; onlyw_k has a known
  integral lift. Global norm-preimage existence still needs
  point-local conditions. Restriction ofw0 has normp^e w0.
  In the separate point subcase the actual norm quotient is
  Hhat^0(Gal(F/K),E(F)⊗Zp)^+/n. No selected point for Kato
  or vanishing of its specific CTP has been proved.
- **CM.** The SINGLE point coefficient augmentation sequence gives
  a UNIQUE eta because H0(K_n,T_pi/p^k)=0 and its original
  twisted augmentation iszero. First moment is
  A1(g)-g(v)⊗kappa_(p^k)(R)(g), withv=-augA(h)/2.
  Its actual tower projection is p² eta, not eta.
  For anyfixedjetd andprecisionk,
  r>=max(n+1,k+floor(log_p d)), m>=r+ceil(k/2)
  forces the ENTIRE fixed-jet imagezero overZ/p^k.
  In the original joint object the norm degree is
  D_m=2(p^m-1); eta supplies the unique next D_m+1 lift.
  The Weil contraction targetsH1(mu_p), notT_pi.
  Rational p^(-2m)/p^(-4m) systems have their indicated
  unbounded lattices; no impossibility of other bounded
  representatives is asserted. Full rho twisting changes
  the quotient endpoint to rho and is not finite trivialization.
  A separate actual higher class has exact orderp^k using
  v_a(L_a)=-12 and unramified division fields at a!=p.
  Its tame residue isnonzero and originalrho transferzero.
  This is not Selmer or c2 nonvanishing.

### Earlier inputs and next action

[Ledger §22](research-ledger.md#22-a-rational-k2-retraction-geometric-determinant-and-actual-isogeny-torsors)
retains the exact rational K2 retraction, full real projected class,
geometric quadratic/cubic point coefficients and ordinary-MHS-map
obstruction, classical native isogeny torsors and prior CM norms.
The §21 exact conditional CM index/sigma recipe and the §20
c2,5=20mod25,70mod125/Sha5=0 certificate remain unchanged.

Read [the next plan](next-research-plan.md). The new targets are
rational arithmetic classes/selection in the now explicit coefficient
geometry, a comparison retaining the original CM ray branch, and
annihilation of the particular Prym/Heegner obstruction.
Do not redo the completed real lift, motivic coefficient object,
finite Poisson formula, fixed-jet theorem or old certificates.
Every new deduction still needs a separate adversarial review.

### Signed Gysin convention from the preceding round

For D(a,b)=(da,f(a)-db) and natural PLUS projection, canonical boundary
is(0,-beta). The project fixed SIGNED partial^+(beta)=(0,+beta) and the
trace positive on it. Thus partial^+=-partial_can and tr_signed=-tr_can.
G_v=-g_v^std is signed as well. Its local representative(0,+beta) maps
to(0,0,-beta,0) in the excision model; it is not the standard positive
divisor orientation. Changing both trace and Gysin preserves values;
changing only one reverses them. Inertia has no Q/K factor; Frobenius
pairing with the K-local test has the separate factor2.

### Audit repairs that must survive compaction

**Heegner normalization.** Never simply apply Howard's local Frobenius
matrix to a global cocycle. The proved global conjugation eigenvalue is
ε_r=(-w(E))(−1)^r. Finite evaluation transforms by Frob_v, transverse
evaluation by −Frob_v. The explicit scalar
u_r=(-w(E))^r(−1)^(r(r−1)/2) normalizes every edge at the full common
coefficient; u₂=−1 preserves two-prime vanishing. The local identity
is a_v−(v+1)F=(F−a_v)(F²−1). This replaces the draft's compressed
induced-automorphism language without strengthening O5's hypotheses.

**CM local/global height.** The normalized GLOBAL K-height has AB
component H/2. That does not construct a selected-embedding local
reference H/2. The actual local reference is C_v. The corrected averaged
determinant has baseline 4det C_v, not Reg_v. C_v=H_v/2 would suffice
for that specialization but is unproved and not necessary. The finite
comparison chooses J=E_ij with coefficient one and the canonical integral
unit difference, rather than reducing an arbitrary rational blend.

**Crystalline Frobenius.** For CM39 at good split p, α is the unit root,
β=p/α. Usual F has eigenvalues β on ω and α on xω. The Tate-twisted
φ=F/p has eigenvalues α⁻¹ and β⁻¹ respectively. BKS's ν is therefore
transverse to ω. The inconsistent printed SW §4.1 label is not used;
MST §3.2 and BKS Lemma 6.9 verify the dictionary. No Euler or height
factor changed. Keep this distinct from the Sen convention, whose
elliptic eigenvalues are 0,1 and whose coefficient deformation has
nonzero nilpotent blocks.

### Further conventions and exact universal obligations

The preceding round's cofactor identity remains
(a_v-F_v)(F_v²-1)=(v+1)F_v-a_v. The full strict Heegner group has
an independent free R direction: the particular class may vanish in a
NONZERO group. Never try to prove the entire strict dual is zero.
Du–Yang is1702.07917v2 (52pages); Sprang1802.04996v2's direct p35
reindexing gives D^(2-w), correcting p31's shorthand D^(1-w).
Trivial Sym0 does not remove Q(1). Ordinary arithmetic codimension3
vanishing is rational/real, not a theorem about integral stack torsion.

PrimeIndex-389 and Leading-389 in §4 are still open. O5/HK-TP5 needs
the PARTICULAR two-prime class to vanish at all relevant full finite
coefficients from the extra complex vanishing. CM-Presentation-BSD
needs ONE rational framed class before separate completions, with
real value ell_E/(2Omega_E) and all fixed local realizations. The
local regulator inverse is only on its specified nonzero line;
undivided formulas remain elsewhere. WTA-389/RGC-389 needs the
rational arithmetic comparison of the now constructed Poisson and relative classes
to D_pt tensor Q beta2 tensor Q(1)^(-2),
with coefficient6N(N-1)n_E as a conclusion, not an input scalar.

The uniform torsor/Bott/connection alternatives still require a
uniform period-independent bound or global Kummer adjustment; fixed
period bundles, finite high-weight lifts and local ordinary blocks
have not supplied it. Read ledger §§9–23 before retrying a shortcut.
None of these test-curve tasks replaces the universal rank, full Sha
and exact leading-formula objective in §1. The latest independent
continuity audit preceded this round; root validates current proof,
review, link and hash routes before refreshing the manifest.

## 6. Computation environment and reproduction

Repository: `/Users/kaveh/bsd-conjecture`. Mathematical source files live in
`docs/`; scripts in `compute/scripts/`; committed-intended data in `compute/data/`.
There is substantial uncommitted work. `.tools/` is ignored and contains the
local Sage 10.7/PARI runtimes and disposable logs. Its caches are not the
sole record of any proof.

Use `DOT_SAGE="$PWD/.tools/sage-home"` so Sage does not try to write outside
the workspace. From the repository root, the original four-certificate dependency order is:

```sh
DOT_SAGE="$PWD/.tools/sage-home" .tools/sage/bin/sage -python compute/scripts/certify_mellin.py
DOT_SAGE="$PWD/.tools/sage-home" .tools/sage/bin/sage -python compute/scripts/certify_bsd_interval.py
DOT_SAGE="$PWD/.tools/sage-home" .tools/sage/bin/sage -python compute/scripts/certify_exceptional_primes.py
DOT_SAGE="$PWD/.tools/sage-home" .tools/sage/bin/sage -python compute/scripts/kurihara_witness.py
```

The last full suite completed with four PASS results on 2026-09-12. Alternate
Mellin cutoffs (96 bits/T=60 vs 128 bits/T=80) and regulator doubling depths
(6 vs 7) gave compatible positive intervals. Those secondary outputs are in
`.tools/research-2026-09-12/` and are supplementary; the principal data and
complete error proofs are in tracked-intended files.

A previous computational round added two independently checked reproducers:

```sh
python3 compute/scripts/arithmetic_surface_check.py
DOT_SAGE="$PWD/.tools/sage-home" .tools/sage/bin/sage -python compute/scripts/certify_cm39.py
```

The first uses the existing 389a1 analytic data; the second writes the new
CM-39 certificate. Both have already passed. The full current script map,
including the earlier eta and coherent-measure checks, is in
[`compute/README.md`](../../compute/README.md). Do not rerun these solely
to recover context.

The NEW CM p-adic certificate was also independently reproduced:

```sh
DOT_SAGE="$PWD/.tools/sage-home" .tools/sage/bin/sage -python compute/scripts/certify_cm39_padic.py
```

It writes cm39_padic_derivative_certificate.json, reads the existing CM
rank certificate and primary table cache, and records their hashes. Its
infinite error theorem and two reviews are in the CM nonvanishing note.
This script needs no rerun merely for context; its new carry arithmetic
was independently checked after the last symbol reproduction.


Known implementation details: Sage's `tate_curve` needs a Sage integer (`ZZ(p)`),
not a plain Python integer; `ComplexBall` finiteness is checked on real and
imaginary parts; `power_mod` with plain Python integers can fail at exponent zero,
so the exact witness uses Python's three-argument `pow`.

## 7. Checkpoint maintenance

Before a long yield or after a material result: update the result and next-action
sections, add an entry to the ledger, revalidate live handles, and refresh the
manifest for changed proof/certificate files. Never mark a task done merely
because an agent's final answer says done; inspect its owned files and proof scope.
Read the complete relevant proof before relying on a compressed table entry.
