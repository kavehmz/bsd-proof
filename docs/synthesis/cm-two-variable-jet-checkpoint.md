# Actual CM two-variable first jets: checkpoint

Date: 2026-09-12. Author /root/odd_rank_bridge, GPT-6 Astra/xhigh.
Own only cm-two-variable-jet-attack.md and this checkpoint.
The parent objective remains full BSD over Q. No new agents or old
numerical certificate reruns.
Status: bounded construction completed; all nine sections passed
[independent coordinator review](review-cm-two-variable-jet.md).

## Completed construction and unchanged scope

**Later result superseding the original local-condition limitation:**
[cm-ray-local-conditions-attack.md](cm-ray-local-conditions-attack.md) and
[its PASS review](review-cm-ray-local-conditions.md) prove BOTH derivatives
and their difference individually Selmer at integral and finite levels.
For the actual E39 their unramified split-place localizations are also
integrally zero. The statements below about only the sum being known
refer to the historical two-jet construction alone, not current status.
The Tor, independence and rational-determinant questions remain open.


The two integral first ray-direction classes have been constructed
from the actual theta units, with exact tame branch, lattice,
smoothing and generator data. The finite class and its derivatives
do not require two-variable H¹ flatness or I-divisibility.
The only proved Selmer statement is for their cyclotomic sum.

The exact ray group has |Δ|=1152(p−1)², a p-unit. The
generators have Tate-character pairs (1+p,1) and(1,1+p).
Both map to the old cyclotomic γ, so X,Y→T and the two
derivatives specialize by their SUM, with no factor1/2. The
diagonal subgroup generated by their product maps instead toγ².
Before the rho twist the source branch is
e_(rho^-1)=|Δ|^-1 sumδ rho(δ)δ; corestriction remains the
unnormalized norm |Δ| times the corresponding averaging projector.

## Completed deductions

Full proofs are saved in cm-two-variable-jet-attack.md §§1–9;
all new propositions passed independent coordinator review.

The actual finite source is U_(r,s)=Theta_a(Omega/(f0 pi^r barpi^s))
on F_(r,s)=K(f pi^r barpi^s). The Δ-fixed field at bi-level(n,k)
is F_(n+1,k+1)^Δ. First-jet maps modulo p^m use n,k≥m,
so EVERY corresponding ray exponent is at least m+1. The unit
coefficient insertion is made at r,s≥m before either direction is
discarded. Norming first to the division field is the old e_(r,s)
construction, by CM reciprocity and transitivity.

The exact two-variable smoothing divisor is
q_a=12(a²−u_a(1+X)^lambda_a(1+Y)^lambda_a),
u_a=Psi^c((a))=±a, lambda_a=log_p(a)/g_p.
Its augmentation is a p-unit, and q_a(T,T) is the old cyclotomic
divisor. The unnormalized tame norm factor |Δ| has been retained.
The fixed Néron Betti coefficient remains2pr_rho(gamma_E^+).

At M_m tensor R_m with R_m=(Z/p^m)[X,Y]/(X,Y)², the
actual normalized ray class has zero augmentation by comparison
with the proved cyclotomic z=T w. The exact coefficient sequence
and H⁰=0 (the tame involution acts−1) give UNIQUE global classes
d_pi,m andd_barpi,m. A cocycle A+XB+YD withA=gv−v gives
B+c_pi gv andD+c_barpi gv. Cochain-choice invariance and
coefficient compatibility are proved. Their integral limits exist;
Shapiro sends their SUM exactly to the old w0. Individual Selmer
membership is not asserted. Generator changes have explicit inverse
scalars on the coefficients and compensating scalars in the quotient.

The coefficient Koszul resolution proves derived base change without
an Iwasawa H¹ flatness assumption. With M=H¹_Iw,N=H²_Iw,
0→Tor₂(N,Z_p)→M/IM→H¹(K,T_piE)→Tor₁(N,Z_p)→0,
and H²_global=N/IN. The intermediate cyclotomic edge retains
N[X−Y]. Thus the actual zero augmentation has NOT been upgraded
to z∈IM. The rank-one free H¹ example with differential(X,Y)
checks exactly this failure, even though its cyclotomic image is
T-divisible; it is not an arithmetic counterexample.

The actual full unit lift also gives new GLOBAL cup constraints:
c_pi cupd_pi=0, c_barpi cupd_barpi=0,
c_pi cupd_barpi+c_barpi cupd_pi=0.
They follow from the actual quadratic coefficients after the constant
coboundary is removed. They do not make Selmer-cone heights vanish
and do not imply local conditions on each derivative.

The final framed formula uses ONLY the Selmer sum and retains
p/(2#E(Fp)), c_cmp=(156iOmega_p)^(-1), e_p=(1−alpha^(-1))²
and Reg_p. The inverse is only on its known line when Reg_p≠0.
The actual local block C_v has not been identified with globalH/2.
GAP CM-TwoJet §8 still requires one rational framed element with
real realizationell_E/(2Omega_E), with rationality as a conclusion.

The coordinator reviewed the actual tame/ray normalization,
finite exponent bound, Koszul edge sequence and global cup constraints.
The reviewed mathematical revision is
e7bcdbbee667a7ebfd65797d11305cfd78e0d85ff5cacd40cbf63393024ee3f7.
The Shapiro map was subsequently renamed Sh to distinguish it from
the ramification set S; that and these PASS links are editorial.

The separately assigned Heegner–Gysin audit is also complete, with
PASS in review-heegner-gysin.md after the canonical-versus-signed
boundary/Gysin convention was repaired. Its Lawson–Wuthrich
irreducible-image extension and exact residue presentation passed.
Do not restart that review as pending work.

The next mathematical target remains GAP CM-TwoJet §8: an
actual rational global framed comparison with the stated real
regulator. No further bounded mathematical task has been dispatched
here. No source review is outstanding, no old script ran, and no
new agent was created.
