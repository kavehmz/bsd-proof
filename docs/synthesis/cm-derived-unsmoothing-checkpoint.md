# CM derived unsmoothing: completed checkpoint

Started: 2026-09-12. Completed review: 2026-09-13.
Author /root/odd_rank_bridge, GPT-6 Astra/xhigh.
Own cm-derived-unsmoothing-attack.md and this checkpoint.
Status: all eight sections completed with
[full independent PASS review](review-cm-derived-unsmoothing.md),
also inspected by the coordinator. No mathematical correction was needed.
Full BSD over Q remains active and unresolved.

Reviewed mathematical proof SHA256:
c37c8b717bea884135fc5a241a6318383f9bfc59c726449821be6bbfe9ac80df.
The old exploratory snapshots are superseded by this completed status.
No prime scan, old certificate rerun, extra agent or shared/completed
proof edit was made. Completion links are editorial.

## Completed actual construction

Inputs are the reviewed CM symbol–point proof and its exact theta,
ray/point-field and norm-root constructions. The old complete-transfer,
first-augmentation and corrected cubical-height zeros were retained as
inputs, not repeated as new targets.

The actual SINGLE point-division torsor Y_R,m=[p^m]^-1R, for
R=P,Q,P+Q, gives the coefficient sequence
0→I_m,k⊗T_pi→A_k[Y_R,m]⊗T_pi→T_pi→0.
The actual theta Kummer class, SAME rho vector, unnormalized transfer
and fixed smoothing unit give a zero-augmentation class. H0(T_pi)=0
makes its lift eta UNIQUE. Its explicit first moment is
A_1(g)−g(v)⊗kappa_(p^k)(R)(g), with v=−augA(h)/2.
The positive point-Kummer coefficient sign and all tensor factors
are retained. This is an actual cone class, not a formal weight derivative.

The single-point theta tower has norm exponent p², and its actual
coefficient projection gives pi_*eta_(m+1)=p²eta_m. Over FULL
A_k=Z/p^k, every fixed order-d point jet vanishes at the explicit depth
r≥max(n+1,k+floor(log_p d)), m≥r+ceil(k/2).
The proof uses v_p binom(p^r,j)=r−v_p(j) and the actual torsor
projections. It does not discard cohomological base-change/Tor terms
or set the entire relative class or growing-order operations to zero.

Modulo p the joint norm-root cocycle starts in degreeD_m=2(p^m−1):
the unused norm is X^(p^m−1)Y^(p^m−1). Its leading class is
kappa_p(w_R) times this canonical line. The original CM branch kills
that coefficient. Replication identifies the unique degreeD_m+1 lift
with the new eta, retaining E[p]⊗T_pi. Its Weil contraction lands
in H1(mu_p), not H1(T_pi). Every fixed active band vanishes by the
full-coefficient theorem.

## Rational normalization and the separate nonzero higher class

The actual untwisted rational Kummer systems are p^(−2m)kappa(u_m)
on the single-point fields and p^(−4m)kappa(u_m) on the joint fields.
Their indicated lattices have unbounded denominators. They are NOT
asserted to lie in a rationalized integral Iwasawa inverse limit.
Both norm to p^(−2m)kappa(w_R) on F_m. Tensoring an actual Kummer
extension withrho changes BOTH endpoints, giving an extension ofrho
byT_pi rather than an extension ofQp byT_pi. Full-character evaluation,
adequate integral lattices and the primitive-ray comparison remain open.
No log(a) or zero smoothing eigenvalue is divided.

The ALREADY fixed rational cube L_a gives a separate actual
constant-unit norm-root torsor with root L_a^(p^(4m−k)).
The only new integer check was v_a(L_a)=−12. At a≠p, the ray and
point-division fields are unramified because E is good and [p^m]
is étale there. The relative class therefore has EXACT orderp^k.
Modulo p its nonzero coefficient is kappa_p(L_a) times the full
norm, in degree4(p^m−1). Its tame residue at the good primea is
nonzero. The full norm line is invariant, so its SAME rho-twisted
transfer is zero in the retained global norm-kernel coefficient.
Neither this auxiliary nonvanishing nor its local boundary is a
claim of an ordinary Selmer class or of nonzero c2,p.

## Sources, review and remaining task

Primary Kato1.3,1.10,15.8 and Nekovář3.4,8.1 were checked by the
independent reviewer in the existing caches
/tmp/cm-derived-kato2004.pdf and /tmp/cm-presentation-nekovar.pdf.
The review reconstructs every coefficient/norm map and the exact
finite threshold. No psi values, regulators, ranks or old certificates
were rerun. No process is running.

The exact remaining target is CM-Derived-Unsmoothing in§8:
an arithmetic comparison retaining the ORIGINAL primitive ray branch,
or a proved rational/locally analytic normalization and integral lattice.
It must give actual unsmoothed point and coefficient/index control,
and one rational frame with local scalarc2/(2e_p detB_p) and real
realization ell/(2Omega_E). Original q_a,iota,u(0),Smith,polarization
and period factors remain fixed. Rationality, all good split primes,
bad/nonsplit primes and full universal BSD remain unproved.

Next action for this CM construction: no expansion; await a new bounded
assignment. The separately assigned marked-coefficient review has a
completed eight-section PASS. Any added motivic-object or real-lift
construction must receive a SEPARATE audit before that verdict extends.
